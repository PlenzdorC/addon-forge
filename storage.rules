rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow all reads and writes for now (can be restricted later)
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}

